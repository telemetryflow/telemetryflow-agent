# TFO Collector Configuration for E2E Tests
#
# TelemetryFlow Agent - AI-Powered Observability & Incident Response Management (IRM) Platform
# Copyright (c) 2024-2026 Telemetri Data Indonesia. All rights reserved.

extensions:
  health_check:
    endpoint: 0.0.0.0:13133

receivers:
  tfootlp:
    protocols:
      grpc:
        endpoint: 0.0.0.0:4317
      http:
        endpoint: 0.0.0.0:4318
        transport: tcp

processors:
  batch:
    send_batch_size: 100
    timeout: 5s

exporters:
  debug:
    verbosity: detailed

  otlp/jaeger:
    endpoint: jaeger:4317
    tls:
      insecure: true

service:
  telemetry:
    logs:
      level: debug
    metrics:
      address: 0.0.0.0:8888

  extensions:
    - health_check

  pipelines:
    traces:
      receivers: [tfootlp]
      processors: [batch]
      exporters: [debug, otlp/jaeger]

    metrics:
      receivers: [tfootlp]
      processors: [batch]
      exporters: [debug]

    logs:
      receivers: [tfootlp]
      processors: [batch]
      exporters: [debug]
