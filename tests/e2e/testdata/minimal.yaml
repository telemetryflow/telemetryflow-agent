agent:
  id: "e2e-test-agent"
  hostname: "test-host"

api:
  endpoint: "http://localhost:3100"

heartbeat:
  interval: 30s

collectors:
  system:
    enabled: true
    interval: 10s

logging:
  level: debug
  format: json